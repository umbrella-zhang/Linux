一、linux网络的三种连接方式
1、桥接模式：
在虚拟机里面的网络地址必须和外部的网络地址保存在同一网段。这样Linux操作系统才可以和外部的机器进行通信。但是如果用户数量太多容器容易造成ip冲突
2、NAT模式（网络地址转换）：
在NAT模式下，虚拟机里面的网络可以不在和主机里面的网络保持在同一网段。但是主机里面会存在虚拟网卡。这个虚拟网卡的ip地址必须和linux里的ip地址在同一网段。这样Linux就可以和主机之间进行通信。同时我们的linux操作系统也可以和外部的网络进行通信。---->解决了ip冲突的问题
3、仅主机模式：
linux的IP地址必须和主机保持一致。

二、虚拟机的克隆
1、直接将原来建好的虚拟机的文件夹复制粘贴，改个名字就可以了
然后点击文件夹里面的.vmx文件,就可以打开了
2、右键vm中的虚拟机，在管理中找到克隆，然后有两种克隆的方式，一种是链接克隆，另一种是完整克隆
链接克隆：对原始虚拟机的引用，相当于还是在原始虚拟机上操作
完整克隆：重新生成一个当前虚拟机的副本，相当于一个独立的虚拟机

三、虚拟机的快照/虚拟机的迁移和删除
右键虚拟机找到快照就行
删除直接右键管理-->从磁盘中删除
迁移就剪切然后粘贴到你想迁移的地方就行









