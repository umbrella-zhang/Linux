/bin目录:是Binary的缩写，这个目录存放着最经常使用的命令，比如cd，cp命令--->直接操作硬件

/sbin目录：s是Super User的意思，这里存放着系统管理员使用的系统管理程序

/home目录：存放着普通用户的主目录，在Linux中每个用户都有一个自己的目录，一般该用户名是以用户的账号命名的

/root:该目录为系统管理员，也就是超级权限者的用户主目录，如果我们使用root账号登录，默认所处的位置就是在/root下面

/lib：系统开机所需要最基本的动态连接共享库，其作用类似于Windows里的DLL文件。几乎所有的程序都要用到这些共享库。如果这个目录里的文件被删除了Linux操作系统也就不能正常运行了

/etc:所有系统管理所需要的配置文件和子目录，Linux系统本身所需要的配置文件也是放在etc目录。如果我们自己安装一些系统服务，比如mysql数据库服务，那么和数据库相关的配置文件（my.cnf）也是放在/etc目录里面

/usr:这是一个非常重要的目录，用户的很多应用程序都在这个目录下，类似于windowns下的program files目录

/usr/local：简单来说就是应用程序安装过后安装程序所存在的目录，一般是通过编译源码的方式安装的程序

/boot:存放的是使用Linux时使用的一些核心文件，包括一些连接文件以及一些镜像文件。如果这个目录里的文件被删除了，Linux也就不能正常运行了。

/dev:类似于Windows的设备管理器，把所有的硬件用文件的形式存储

/proc:这个目录是一个虚拟目录，他是系统内存的映射，访问这个目录来获取系统的信息。这个目录我们不要动，否则可能造成系统的崩溃

/srv:service的缩写，该目录中存放的一些服务，用来服务启动之后所需要提取的数据。这个目录我们不要动，否则可能造成系统的崩溃

/sys:这个目录存放了Linux内核相关的文件信息。这个目录我们不要动，否则可能造成系统的崩溃

/tmp:这个目录是用来存放一下临时文件的。

/media:linux系统会自动识别一些设备，例如U盘，光驱等等，当识别后，Linux会把识别的设备挂载到这个目录下。比如我们插入的磁盘光驱都会映射成对应的文件放在media目录

/mnt:系统提供该目录是为了让用户挂载别的文件系统的，我们可以将外部的存储挂载到/mnt/上，然后就可以进入该目录查看里面的内容了。比如windows和linux的文件共享操作

/opt:这是主机给安装软件所存放的目录，如果安装jdk,可放到该目录下默认为空

/var:这个目录中存放着不断变化和扩充着的东西，最常用的就是各种日志文件




